#
---
services:
  acmecgi:
    image: acmecgi:0.2.1
    ports:
      - "10443:8443/tcp"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
      - ./config:/app/config

