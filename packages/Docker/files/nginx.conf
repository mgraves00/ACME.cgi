# /etc/nginx/nginx.conf

user nginx;

# Set number of worker processes automatically based on number of CPU cores.
worker_processes auto;

# Enables the use of JIT for regular expressions to speed-up their processing.
pcre_jit on;

# Configures default error logger.
#error_log /app/logs/error.log warn;
error_log /dev/stdout warn;

events {
	# The maximum number of simultaneous connections that can be opened by
	# a worker process.
	worker_connections 1024;
}

http {
	# Includes mapping of file name extensions to MIME types of responses
	# and defines the default type.
	include /etc/nginx/mime.types;
	default_type application/octet-stream;

	# Don't tell nginx version to the clients. Default is 'on'.
	server_tokens off;

	# Specifies the maximum accepted body size of a client request, as
	# indicated by the request header Content-Length. If the stated content
	# length is greater than this size, then the client receives the HTTP
	# error code 413. Set to 0 to disable. Default is '1m'.
	client_max_body_size 10m;

	# Sendfile copies data between one FD and other from within the kernel,
	# which is more efficient than read() + write(). Default is off.
	sendfile on;

	# Causes nginx to attempt to send its HTTP response head in one packet,
	# instead of using partial frames. Default is 'off'.
	tcp_nopush on;

	# Helper variable for proxying websockets.
#	map $http_upgrade $connection_upgrade {
#		default upgrade;
#		'' close;
#	}

	# Specifies the main log format.
	log_format main '$remote_addr - $remote_user [$time_local] "$request" '
			'$status $body_bytes_sent "$http_referer" '
			'"$http_user_agent" "$http_x_forwarded_for"';

	# Sets the path, format, and configuration for a buffered log write.
#	access_log /app/logs/access.log main;
	access_log /dev/stdout main;

	ssl_session_cache	shared:SSL:10m;
	ssl_session_timeout	10m;

	server {
		listen 8443 ssl;
		listen [::]:8443 ssl;

		ssl_certificate	/app/config/certs/server.fullchain.pem;
		ssl_certificate_key /app/config/certs/server.key;
		ssl_password_file /app/config/certs/server.pass;
		ssl_protocols	TLSv1.2 TLSv1.3;
		ssl_ciphers	HIGH:!aNULL:!MD5;

		index index.html;
		root /app/html;

		location ~ /acme/* {
			root /;
			include /app/config/fastcgi_params;
			fastcgi_pass 127.0.0.1:9000;
			fastcgi_param SCRIPT_NAME /app/cgi-bin/ACME.cgi;
		}
		location ~ /ca/* {
			root /app/data;
			autoindex off;
		}
	}
}
